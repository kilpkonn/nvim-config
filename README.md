# kilpkonn nvim config
_Originally based on: https://github.com/NvChad/NvChad, but now heavily modified_

## Installation
Clone the repository and symlink it:

NuShell
```nu
ln -s -f (pwd) ~/.config/nvim
```

Bash
```bash
ln -s -f $(pwd) ~/.config/nvim
```
